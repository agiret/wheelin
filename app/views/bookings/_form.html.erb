<h2><%= @wheely.title %></h2>
<h3><%= @wheely.price%> €/jr </h3>

<%= simple_form_for [@wheely, @booking] do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :booking_date, data: { provide: "datepicker" }%>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
