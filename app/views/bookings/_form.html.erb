
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      <div class="card-form">

          <h1>Book a Wheely</h1>
            <h2><%= @wheely.title %></h2>
            <h3><%= @wheely.price%> €/jr </h3>

            <%= simple_form_for [@wheely, @booking], html: { class: 'form-horizontal' } do |f| %>
              <%= f.error_notification %>

              <div class="form-inputs">
                <%= f.input :booking_date, as: :string %>
              </div>

              <div class="form-actions">
                <%= link_to 'Cancel', :back, class: "btn btn-default"%>
                <%= f.button :submit, 'Book', class: "btn btn-success pull-right" %>
              </div>
            <% end %>
      </div>
    </div>
  </div>
</div>
